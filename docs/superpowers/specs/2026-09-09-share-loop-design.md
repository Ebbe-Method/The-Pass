# The Pass — spec 4: personalized share loop

Date: 2026-09-09
Status: fixture OG + README badge shipped. Live per-repo OG needs a hosted renderer.

## Problem

The only growth loop until Sean Ellis ≥ 40% is: see a still → open demo → magic → install → share *their* board still.

## Approach

- Spec 1 already ships fixture `og.svg` and a 15-second silent loop note.
- Spec 4 adds `/api/badge?owner=&repo=` SVG and README markdown.
- Personalized OG of their board: titles may blur. Hosted after the App has data.

No paid ads, invites, sounds-on, or leaderboards until HXC ≥ 40%.

## Success

A README badge that links the kiosk. A still of their board that a stranger would open.
